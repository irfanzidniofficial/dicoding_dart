void main() {
  // Positional Optional Paremeter
  // void greetNewUser([String name, int age, bool isVerified]);

  // greetNewUser('Windy', 12, true);

  // // Named Paremeter
  // void greetNewUser({required String name, required int age, bool isVerified = false}) {}
}
